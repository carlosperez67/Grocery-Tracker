package ui.buttons.click;

import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

// New Class to Handle Clicking on a Save Button
public class SaveButtonClickHandler implements ActionListener {

    // EFFECTS: saves grocery app status into json
    @Override
    public void actionPerformed(ActionEvent e) {
        // TODO (create action for save button)
    }
}
