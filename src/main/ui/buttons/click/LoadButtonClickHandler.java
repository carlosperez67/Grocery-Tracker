package ui.buttons.click;

import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

// New Class to Handle Clicking on a Load Button
public class LoadButtonClickHandler implements ActionListener {

    // EFFECTS: loads grocery app status from saved json
    @Override
    public void actionPerformed(ActionEvent e) {
        // TODO (create action for load button)
    }
}